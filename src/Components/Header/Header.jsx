import React from 'react';
import Nav from '../NavBar/Nav';

const Header = () => {
    return (
        <div className='container-xl'>
            <Nav />
        </div>
    );
}

export default Header;
